{
  "$schema": "https://vega.github.io/schema/vega-lite/v6.json",
  "description": "A trellis split by buckets of a column, which the facet has to bucket itself: its bin belongs to the grid rather than to a cell, and both ends of the bucket name the row.",
  "data": {"url": "data/cars.json"},
  "mark": "point",
  "encoding": {
    "row": {"field": "Acceleration", "bin": true},
    "x": {"field": "Horsepower", "type": "quantitative"},
    "y": {"field": "Miles_per_Gallon", "type": "quantitative"}
  }
}
